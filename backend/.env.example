DEBUG=True
SECRET_KEY=change-me-to-a-long-random-string
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=postgres://shivlal:shivlal@localhost:5432/shivlal_db
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
CORS_ALLOWED_ORIGINS=http://localhost:5173
MEDIA_ROOT=media/

# ── Email (password reset) ───────────────────────────────────────────────────
# Dev defaults to the console backend (emails print to the terminal) — no setup
# needed locally. To test real Gmail sending in dev, uncomment and fill these:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-gmail-address@gmail.com
# EMAIL_HOST_PASSWORD=your-16-char-app-password
# DEFAULT_FROM_EMAIL=Shiv Lal Manpower <your-gmail-address@gmail.com>
FRONTEND_URL=http://localhost:5173
PASSWORD_RESET_TIMEOUT=7200
